import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cmpm121.orechardoutlet',
  appName: 'Orechard Outlet',
  webDir: 'docs'
};

export default config;
